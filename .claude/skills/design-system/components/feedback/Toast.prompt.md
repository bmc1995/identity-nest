Transient notification with semantic tone + icon. Stack inside `<ToastRail>` (fixed bottom-right).

```jsx
<ToastRail>
  <Toast tone="success" title="Client created" onClose={dismiss}>nest_a91f2e is live.</Toast>
  <Toast tone="danger" title="Sign-in failed">Invalid client secret.</Toast>
</ToastRail>
```
